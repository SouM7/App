const express = require('express');
const router = express.Router();
const ExoController = require('../Controllers/Exo.controller');

//router.get('/exos', ExoController.getAllExos);
//router.get('/:id', ExoController.getSingleExo);
//router.post('/', ExoController.createExo);
//router.put('/:guid',ExoController.updateExo);
// router.put('/:id', ExoController.editExo);
//router.delete('/:id', ExoController.deleteExo);

module.exports = router;